<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bob.modules.user.mapper.UserMapper">
	<resultMap id="BaseResultMap" type="com.bob.modules.user.model.User">
		<result column="id" property="id" />
		<result column="org_id" property="orgId" />
		<result column="org_code" property="orgCode" />
		<result column="user_name" property="userName" />
		<result column="pass_word" property="passWord" />
		<result column="nick_name" property="nickName" />
		<result column="user_role" property="userRole" />
		<result column="is_delete" property="isDelete" />
		<result column="create_time" property="createTime" />
		<result column="create_user" property="createUser" />
		<result column="update_time" property="updateTime" />
		<result column="update_user" property="updateUser" />
	</resultMap>

	<sql id="tabaleName">user</sql>

	<sql id="baseColumn">
		id,
		org_id,
		org_code,
		user_name,
		pass_word,
		nick_name,
		user_role,
		is_delete,
		create_time,
		create_user,
		update_time,
		update_user
	</sql>

	<select id="getList" parameterType="com.bob.biz.user.model.UserVo"
		resultMap="BaseResultMap">
		select
		<include refid="baseColumn"></include>
		from
		<include refid="tabaleName"></include>
		<where>
			<if test=" orgId !=null and orgId !='' ">
				and org_id = #{orgId}
			</if>
			<if test=" orgCode !=null and orgCode !='' ">
				and org_code like '#{orgCode}%'
			</if>
			<if test=" userName !=null and userName !='' ">
				and user_name like '%#{userName}%'
			</if>
			<if test=" passWord !=null and passWord !='' ">
				and pass_word = #{passWord}
			</if>
			<if test=" nickName !=null and nickName !='' ">
				and nick_name like '%#{nickName}%'
			</if>
			<if test=" userRole !=null and userRole !='' ">
				and user_role = #{userRole}
			</if>
			<choose>
				<when test=" isDelete!=null and isDelete!='' ">
					and is_delete = #{isDelete}
				</when>
				<otherwise>
					and (is_delete is NULL or is_delete = 0 )
				</otherwise>
			</choose>
		</where>
	</select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.biz.category.mapper.CategoryMapper">
	<resultMap id="BaseResultMap" type="com.mall.biz.category.model.Category">
	</resultMap>

	<sql id="tabaleName">category</sql>

	<sql id="baseColumn">
		id,
		name,
		code,
		description,
		is_delete,
		create_time,
		create_user,
		update_time,
		update_user
	</sql>

	<select id="select" parameterType="com.mall.biz.category.model.CategoryVo"
		resultMap="BaseResultMap">
		select
		<include refid="baseColumn"></include>
		from
		<include refid="tabaleName"></include>
		<where>
			<if test=" name!=null and name!='' ">
				and name like '%${name}%'
			</if>
			<if test=" code!=null and code!='' ">
				and code like '%${code}%'
			</if>
			<if test=" description!=null and description!='' ">
				and description like '%${description}%'
			</if>
			<choose>
				<when test=" isDelete!=null and isDelete!='' ">
					and is_delete = #{isDelete}
				</when>
				<otherwise>
					and (is_delete is NULL or is_delete = 0 )
				</otherwise>
			</choose>
		</where>
		<if test=" sidx!=null and sidx!='' ">
			order by #{sidx}
		</if>
		<if test=" sord!=null and sord!='' ">
			#{sord}
		</if>
	</select>

</mapper>
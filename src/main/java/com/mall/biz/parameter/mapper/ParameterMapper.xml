<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mall.biz.parameter.mapper.ParameterMapper">
	<resultMap id="BaseResultMap" type="com.mall.biz.parameter.model.Parameter">
		<result column="id" property="id" />
		<result column="category_id" property="categoryId" />
		<result column="name" property="name" />
		<result column="description" property="description" />
		<result column="is_delete" property="isDelete" />
		<result column="create_time" property="createTime" />
		<result column="create_user" property="createUser" />
		<result column="update_time" property="updateTime" />
		<result column="update_user" property="updateUser" />
	</resultMap>

	<sql id="tabaleName">parameter</sql>

	<sql id="baseColumn">
		id,
		category_id,
		name,
		description,
		is_delete,
		create_time,
		create_user,
		update_time,
		update_user,
	</sql>

	<select id="select" parameterType="com.mall.biz.parameter.model.ParameterVo"
		resultMap="BaseResultMap">
		select
		<include refid="baseColumn"></include>
		from
		<include refid="tabaleName"></include>
		<where>
			<if test=" categoryId!=null and categoryId!='' ">
				and category_id = categoryId,
			</if>
			<if test=" name!=null and name!='' ">
				and name like '%${name}%'
			</if>
			<if test=" description!=null and description!='' ">
				and description like '%${description}%'
			</if>
			<if test=" isDelete!=null and isDelete!='' ">
				and is_delete = isDelete,
			</if>
		</where>
		<if test=" sidx!=null and sidx!='' ">
		</if>
		<if test=" sord!=null and sord!='' ">
			#{sord}
		</if>
	</select>

</mapper>